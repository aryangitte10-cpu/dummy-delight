import { EventsSearch } from '@/components/events-search'

export const metadata = {
  title: 'Events Search | Impact Board',
  description: 'Explore and find events that match your interests',
}

export default function EventsPage() {
  return <EventsSearch />
} 